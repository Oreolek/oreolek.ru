<!doctype html>
<html lang="ru">
  <head>
    <title>{{title}}</title>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <link href='{{favicon}}' rel='shortcut icon' type='image/x-icon'/>
    {{#feeds}}
    <link href="{{{url}}}" rel="alternate" type="application/rss+xml" title="{{title}}">
    {{/feeds}}
    <!--[if lt IE 9]>
{{{ie_scripts}}}
    <![endif]-->
    {{{stylesheet}}}
   </head>
  <body vocab="http://schema.org">
    {{#navigation}}
      <nav class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
              <span class="sr-only">{{text_navigation}}</span>
              <span class="fa fa-volume-off">&nbsp;</span>
            </button>
            <a class="navbar-brand" href="/">{{site_title}}</a>
          </div>
          <div class="collapse navbar-collapse" id="navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
              {{#dropdown}}
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{name}} <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    {{#links}}
                      <li>{{{.}}}</li>
                    {{/links}}
                  </ul>
                </li>
              {{/dropdown}}
            {{#links}}
              <li>{{{.}}}</li>
            {{/links}}
            </ul>
            {{{search}}}
          </div>
        </div>
      </nav>
    {{/navigation}}
    <div class="container">
      <div class = "main_content">
        <div class="paging">
          {{{get_paging}}}
        </div>
        <h2>{{title}}</h2>
        {{#flashes}}
          {{#info}}
            <div class="alert alert-info">{{{.}}}</div>
          {{/info}}
          {{#error}}
            <div class="alert alert-danger">{{{.}}}</div>
          {{/error}}
          {{#warning}}
            <div class="alert alert-warning">{{{.}}}</div>
          {{/warning}}
          {{#success}}
            <div class="alert alert-success">{{{.}}}</div>
          {{/success}}
        {{/flashes}}
{{>content}}
        <div class="paging">
          {{{get_paging}}}
        </div>
      </div>
      <div class="sidebar">
        <div class="col-md-12">
          {{#navigation}}
          <a href="/"><h1 class="navbar-brand" href="/">{{site_title}}</h1></a>
          <div class="rss">{{{rss_link}}}</div>
          <div class="blog-author clearfix">
            <p>
              <img alt="Author" src="{{{author_img}}}" class="img-responsive" height="320" width="240">
              {{author_about}}
            </p>
          </div>
          <ul>
            {{#dropdown}}
            <li>
              <a href="#">{{name}}</a>
              <ul>
                {{#links}}
                  <li>{{{.}}}</li>
                {{/links}}
              </ul>
            </li>
            {{/dropdown}}
            {{#links}}
            <li>{{{.}}}</li>
            {{/links}}
          </ul>
          {{{search}}}
        </div>
        {{/navigation}}
      </div>
      <footer><div class="small">Сайт сделан на фреймворке Kohana. <a href="https://github.com/oreolek/oreolek.ru">Исходный код</a> движка доступен по лицензии <a href="http://www.gnu.org/licenses/agpl-3.0.html">AGPL</a>.</div></footer>
    </div>
{{{scripts}}}
  </body>
</html>
